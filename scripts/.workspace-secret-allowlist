# scripts/.workspace-secret-allowlist — check-secrets-workspace.sh 的逐条豁免表
# （仓内那道门读的是 scripts/.secret-allowlist，两张表互不影响，判据同源）
#
# 语义（见 scripts/check-secrets-workspace.sh 的 is_allowed）：每行一条 ERE，非注释行用 | 串起来，
# 拿命中行的整串 `path:lineno:content` 去 grep -qE。不存在/空文件＝零豁免，任何命中都 FAIL。
#
# 为什么只能逐值豁免、不开「某个 md 文件整类放行」口子：
#   被扫的三层目录（<工作区根>/scripts、docs、cold-start）不进版本库，正是 2026-09-19 审计 F2
#   那批明文凭证藏身的地方 —— 那里出现一个真凭证的概率不比仓内低。整文件豁免等于把
#   "审计报告里引用过凭证形状" 变成 "这份文档以后写什么都不看"，门就恒绿了。
# 每条必须自带「文件 + 变量名 + 值前缀」三段锚点；换一个值就得重新显式登记。
# 另：本表自身在仓内扫描范围内（scripts/ 属 git 区，由 check-secrets.sh 看），所以豁免式不能写成
#   `Xxx = "值"` 的形状 —— 那样 PATTERN 会先命中这一行自己（.secret-allowlist 2026-09-19 实测过一次），
#   三段之间用 .* 跳过即可。

# <工作区根>/docs/audit-2026-09-19-sessionC.md 第 919/1081 行是审计记录，逐字引用了 nm-host
# 脱敏用例的自造 bh_ 夹具值（该值的原件已在 scripts/.secret-allowlist 按"文件+变量名+完整值"登记，
# 064c6a21，2026-09-20）；文档里的引用被截断成 `…` 结尾，故值前缀取到 supersecretvalue 为止，
# 同时覆盖原件的完整值 `bh_26_supersecretvalue0123456789abcdef`（同一个已知假夹具，非两种东西）。
# 真实 host token 由 --token 参数 / 环境变量注入，不经这个常量。
docs/audit-2026-09-19-sessionC\.md:[0-9]+:.*testSecretToken.*bh_26_supersecretvalue
