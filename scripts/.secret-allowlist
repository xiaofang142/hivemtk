# scripts/.secret-allowlist — check-secrets.sh 的逐条豁免表
# （工作区那道门读的是 scripts/.workspace-secret-allowlist，两张表互不影响）
#
# 语义（见 scripts/check-secrets.sh 的 is_allowed）：每行一条 ERE，非注释行用 | 串起来，
# 拿命中行的整串 `path:lineno:content` 去 grep -qE。空文件＝零豁免，任何命中都 FAIL。
#
# 为什么只能逐值豁免、不开 `*_test.go` 整类口子：
#   本仓真实的凭证外泄事故（bulk_seed.py / geo_full_test.py / deep_lib）里，
#   出事文件名本身就带 test —— "在测试文件里"从来不是"这个值是假的"的证据。
#   整类豁免会把闸门变成 gitleaks 之外第二道恒绿的假门，正是本项目反复清理的那类门。
#
# 每条必须自带「文件 + 变量名 + 完整值」三段锚点，窄到只覆盖那一行夹具；
# 换文件、换变量名、换一个不像夹具的值，都得重新显式登记。
# 另：本表自身也在扫描范围内，所以豁免式不能写成 `Xxx = "值"` 的形状——那样
# PATTERN 会先命中这一行自己（2026-09-19 实测过一次），三段之间用 .* 跳过即可。

# nm-host 脱敏用例的自造 bh_ token：值是"supersecretvalue"+字母表序列，用途是断言
# dialAddr 与日志里不出现明文（064c6a21，2026-09-20）。真实 host token 由 --token
# 参数 / 环境变量注入，不经这个常量。
cmd/nm-host/main_test\.go:[0-9]+:.*const testSecretToken.*bh_26_supersecretvalue0123456789abcdef
