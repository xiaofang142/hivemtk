# nm-host 脱敏用例的自造 bh_ token：值是"supersecretvalue"+字母表序列，用途是断言
# dialAddr 与日志里不出现明文（064c6a21，2026-09-20）。真实 host token 由 --token
# 参数 / 环境变量注入，不经这个常量。
cmd/nm-host/main_test\.go:[0-9]+:.*const testSecretToken.*bh_26_supersecretvalue0123456789abcdef

# ── 批C/批G-2b 的三段入站验签夹具（随 1af6d28c 入库，2026-09-22 批M-10 逐值登记）──
# 三条都由 B 段（字面量形状扫描）报出，A 段（本机 .env 真值比对）对这三条一律未命中：
# 值不在 .env 的任何凭证键里，也不是任何真实服务的凭证。逐条核过的形状：
#  1-2 是"名字即值"的自描述串（tt_client_secret_http_d04 / tt_client_secret_d04），
#     真实 tiktok／抖音 app_secret 是 32 位随机串且不含 `_d04` 这类用例编号；
#  3   是 httptest 假服务器自有 URL 上的一段（`a.srv.URL + "/im/media?secret=…%3D"`），
#     对端是自己起的进程，值只用来逼近官方示例里"query 已带转义 ="的形态（见该文件 :207 注释）。
# 登记口径沿用本表既有规矩：三段锚点（文件 + 变量名/函数名 + 完整值），且**不引入新的常量名**
# ——换文件、换形状、或把值改成不像夹具的真凭证，都要重新显式登记。
# 为什么在此逐值豁免而不是把夹具改成带 `test_` 前缀（那会被 NEGATE 表直接放过）：
# "看着像真凭证"正是这些用例存在的理由——它们要证明验签口在真实形状下不松口；把值改软等于削弱用例。
webhook_batchc_d04_tiktok_http_test\.go:[0-9]+:.*d04Secret.*tt_client_secret_http_d04
webhook_batchc_d04_tiktok_test\.go:[0-9]+:.*d04TiktokSecret.*tt_client_secret_d04
webhook_batchg2b_douyin_media_test\.go:[0-9]+:.*dlURL.*skv2Z1fhiKw1gGBsGa4
