# ============================================================
# 用户端 (HiveMtk) - .gitignore
# 私域独立部署仓库（2026-07-21 拆分）
# ============================================================

# ---------- 依赖 ----------
node_modules/
vendor/
__pycache__/
*.pyc
*.pyo

# ---------- 环境配置（含敏感信息）----------
.env
.env.local
.env.*.local
*.env
# 说明：.env 为本地真实配置（忽略）；.env-example 为示例模板，纳入版本追踪。
# 例外：推理栈模型定义文件（非敏感配置，需纳入版本追踪）
!scripts/inference-host/models.env

# ---------- 日志 ----------
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.workbuddy-ai/*
# 例外：审计变异电池的逐格取证产物要能在仓库里找回（读数见 CHANNEL_INTEGRATION_AUDIT_2026-09 §23.19 第 1-补 段）。
# 四条一组的写法是 gitignore 语义逼出来的：① 父目录被 `logs/` 排除时，对其内任何 `!` 规则都不生效，
# 所以先放行 `logs` 本身；② 一放行，该树下**别的泳道/别的轮次**的目录（共享树现数 18 个，含 .txt/.md/bak/*.orig 残本）
# 会一起变成可跟踪 ⇒ 再用 `logs/*` 把直接子项整体挡回；③ 逐个放行本泳道自己的轮次（**一对两轮次目录行，一放行一轮**；
# 轮数这里刻意不写死：它随每批新轮次涨，写死必漂——本行原写"当前 10 轮"，而放行清单加到 `CIwiring` 时磁盘已是 11 轮，
# 那个 10 就是这么过期的。要现数就 `grep -c '^!docs.*logs/[^*]*/$' .gitignore`，与 `ls docs/.../ledger/logs | wc -l` 对齐。
# 加行仍按老规矩：新轮次由跑它的那一批自己加两行，本批不代签别人）；
# ④ 最后按 `*.log` 放行格产物（白名单是"轮次目录"而不是后缀：这些目录内的非 .log 也会入库）。
# 运行时日志（仓库任意其他 logs/ 目录与其他位置的 *.log）不受这四条影响。
!docs/superpowers/specs/ledger/logs/
docs/superpowers/specs/ledger/logs/*
!docs/superpowers/specs/ledger/logs/R30/
!docs/superpowers/specs/ledger/logs/R30/**/*.log
!docs/superpowers/specs/ledger/logs/R28/
!docs/superpowers/specs/ledger/logs/R28/**/*.log
!docs/superpowers/specs/ledger/logs/B16ledger/
!docs/superpowers/specs/ledger/logs/B16ledger/**/*.log
!docs/superpowers/specs/ledger/logs/B16ledgerB/
!docs/superpowers/specs/ledger/logs/B16ledgerB/**/*.log
!docs/superpowers/specs/ledger/logs/B16ledgerC/
!docs/superpowers/specs/ledger/logs/B16ledgerC/**/*.log
!docs/superpowers/specs/ledger/logs/P503/
!docs/superpowers/specs/ledger/logs/P503/**/*.log
!docs/superpowers/specs/ledger/logs/P701/
!docs/superpowers/specs/ledger/logs/P701/**/*.log
!docs/superpowers/specs/ledger/logs/B17action/
!docs/superpowers/specs/ledger/logs/B17action/**/*.log
!docs/superpowers/specs/ledger/logs/R22/
!docs/superpowers/specs/ledger/logs/R22/**/*.log
!docs/superpowers/specs/ledger/logs/P702hook/
!docs/superpowers/specs/ledger/logs/P702hook/**/*.log
!docs/superpowers/specs/ledger/logs/CIwiring/
!docs/superpowers/specs/ledger/logs/CIwiring/**/*.log
!docs/superpowers/specs/ledger/logs/CapAB/
!docs/superpowers/specs/ledger/logs/CapAB/**/*.log

# ---------- 运行时产物（PID / 进程日志）----------
.runtime/

# ---------- 编辑器 / IDE ----------
.vscode/
.idea/
.claude/
.trae/
.sisyphus/
.codebuddy/
.codeartsdoer/
.opencode/
.arts/
.freebuff/
.planning/
.playwright-cli/
*.swp
*.swo
*~

# ---------- 操作系统 ----------
.DS_Store
Thumbs.db
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# ---------- 构建产物 ----------
# dist 目录内容忽略，但保留 .gitkeep 占位
**/dist/*
!**/dist/.gitkeep
build/
*.exe
*.app
# Go 服务二进制
/user-server/cmd/api/api
/user-server/cmd/*/api
/user-server/api
/user-server/merchant_server
/user-server/marketing
/user-server/perf
/user-server/user-server
/user-server/reset-admin
/user-server/main-bin
/user-server/tmp/
/user-server/embedding-server
/user-server/bin/
/user-server/internal/plugin/main/*.so
/user-server/internal/*.so

# i18n 生成缓存
*.fill_cache.json

# 图片资源（体积大，不纳入版本追踪；本地保留即可）
# 例外：README 展示截图 docs/screenshots/screenshot-*.png 必须入库
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.ico
!docs/screenshots/screenshot-*.png

# ⚠️ 例外（构建/运行时**必需**的静态资源，必须入库）
# `*.png` 是未锚定的裸模式，会命中任意层级。以下四类资源缺失会导致产物损坏：
#   1. public/logo.png —— index.html 的 favicon 与 manifest.webmanifest 的 PWA 图标
#   2. bridge/assets/icons/*.png —— Chrome 扩展 manifest.json 的 icons/action 图标，
#      缺失时扩展无法加载（浏览器直接报 manifest 校验失败）
#   3. browser_automation/assets/icons/*.png —— 同上，另一扩展
#   4. website/public/wechat.jpg —— 官网页脚微信二维码（FooterSection.vue 的 :src）。
#      它在旧仓（hivemtk-platform/website）里同样是 `*.jpg` 命中的未跟踪文件，只在
#      部署机上存在；迁入本仓后若不豁免，CI 的 git checkout 拿不到它，
#      website/deploy.sh 会在「dist/wechat.jpg 缺失」这一步当场红。
!user-web/public/logo.png
!user-web/bridge/assets/*.png
!user-web/bridge/assets/icons/*.png
!user-web/browser_automation/assets/icons/*.png
!website/public/wechat.jpg

# 说明：user-web/src/assets/logo.png 与 public/logo.png 字节完全相同（MD5 一致）
# 且全仓无任何引用，属冗余副本，按上述「大图不入库」策略继续忽略。
# 离线包构建产物
dist-offline/

# ---------- 测试覆盖 ----------
coverage/
*.cover
*.cover.out
*.out
*.test
*.prof
playwright-report/
playwright-report-api/
test-results/

# ---------- 临时文件 ----------
tmp/
temp/
uploads/
downloads/
.data/
.round
.round.*
# Vite 转译 vite.config.js 时产生的临时文件（正常会被自动删除，
# 异常退出时会残留；内容含开发者本机绝对路径，禁止入库）
*.timestamp-*.mjs

# ---------- 数据库 / 持久化数据 ----------
*.db
data/
# PG 数据绑定挂载目录（docker-compose ./pg_data）
pg_data/
# 模型文件（./models/ 实际不提交，仅 README 与 .gitkeep）
models/

# ---------- Docker ----------
docker-compose.override.yml
# 说明：docker-compose.yml 为宿主机部署版编排（仅数据层 PG+Redis），纳入版本追踪。
#       真实密钥通过 .env（被忽略）注入，编排文件不保留敏感配置。

# ---------- 证书和密钥 ----------
*.pem
*.key
*.crt
*.p12

# ---------- 备份与缓存 ----------
*.bak
*.backup
*.bak.*
backups/

# ---------- 编译产物（通用）----------
main
*.dll
*.so
*.dylib
# 打包 / 发布产物（由 CI 或本地构建生成，不纳入版本追踪）
*.zip
*.tar.gz

# ---------- 一次性调试产物 ----------
debug-*.html
reset-*.html
clear-*.html
fix-api-config*.html
debug-*.js
debug-*.sh
test_all_apis*.sh
test-all-apis*.sh

# ---------- 临时 restore 目录 ----------
user-server/internal/service/restore_tmp/

# ---------- 运行时授权文件 ----------
merchant.key
install.lock

# ---------- 嵌入 SDK 构建产物 ----------
/embed-sdk/dist/

# ---------- 开发工具配置 ----------
.air.toml
# 例外：user-server/.air.toml 必须入仓 —— HOT_RELOAD.md §三 明写「已入仓，所有开发者
# 共享同一份」，且 `make dev` 走 `cd user-server && air` 依赖它（缺文件时 air 退化为
# 内置默认：不 source ../.env、不监听 .yaml/.html）。2026-09-20 第二十二轮实测：
# 该文件此前从未被追踪，本文档的承诺对干净 checkout 不成立。
!user-server/.air.toml

# ---------- 生成产物（机器生成，不应纳入版本追踪）----------
# 自动生成的代码/接口清单文档（由脚本重新生成）
user-server/NEW_FUNCTIONS_INVENTORY.md
# scripts/api-inventory.sh 的快照产物（CI 以 artifact 上传，非源码，2026-09-16 补）
/api-inventory.md
# 前端测试 / UI 扫描生成的产物与缓存（由脚本重新生成）
user-web/tests/API_TEST_RESULTS.json
user-web/tests/API_INVENTORY.json
user-web/tests/.api_id_cache.json
user-web/docs/ui-inventory/inventory.json
# 页面级端到端审计脚手架运行时产物（由脚本重新生成，非源码）
user-web/tests/audit/reports/
user-web/tests/e2e/audit/reach/
user-web/tests/e2e/audit/summary.json
# 临时运行日志（前端桥接运行输出，非源码）
user-web/_bridge_run.txt

# simulate 运行时数据（脚本产物，非源码）
*.jsonl

# ---------- 内部/敏感文档：不对外公开，仅本地留存于仓库外 internal-docs/ ----------
# 含内部需求/设计逻辑/审计结论/真实客户数据，禁止进入公开仓库。
customer-service/
docs/audit/
docs/bridge/
user-server/docs/audit/
.research/
*-research.md
internal-docs/
.env.geo.local
user-server/geo-run

# ---------- 编译产物（62MB 已 commit 过，GitHub warning）----------
mtk-serve
*.exe

# ---------- 模型数据文件（>1GB，不应进 git）----------
*.gguf
*.mlx
*.safetensors
*.bin
*.pt

# ---------- Go 编译产物（禁止入库）----------
user-server/nm-host
